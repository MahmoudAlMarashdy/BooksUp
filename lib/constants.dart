import 'package:flutter/material.dart';

const primaryColor = Color(0xff100B20);
const transitionDuration = Duration(milliseconds: 500);
const featuredBooksBox = 'Featured Box';
const newestBooksBox = 'Newest Box';